# Docker
# Build and push an image to Azure Container Registry
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- master

resources:
- repo: self

variables:
- group: jsbr_variable_group
  # Container registry service connection established during pipeline creation

  # Agent VM image name
  vmImageName: 'ubuntu-latest'

stages:
- stage: Build
  displayName: Build and push stage
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: $(vmImageName)
    steps:
    - task: Maven@4
      displayName: 'Maven pom.xml'
      inputs:
        goals: 'clean package -DskipTests=false'
        testRunTitle: 'Testing the application code base'
    - task: Docker@2
      displayName: Build and push an image to container registry
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: $(dockerfilePath)
        containerRegistry: $(dockerRegistryServiceConnection)
        tags: |
          $(tag)
    - task: AzureRmWebAppDeployment@4
      inputs:
        ConnectionType: 'AzureRM'
        azureSubscription: '$(azureSubscription)'
        appType: 'webAppContainer'
        WebAppName: 'jsbr-deployment-webApp'
        DockerNamespace: '$(DockerNamespace)'
        DockerRepository: 'wahabajasco/jsbr-deployment'
        DockerImageTag: '$(Build.BuildId)'
        StartupCommand: 'java -jar react-and-spring-data-rest-0.0.1-SNAPSHOT.jar'
        AppSettings: '-Port 8080'
        ConfigurationSettings: 'openjdk:11-jdk-slim-buster'
